; Copy this file to scripts/db_config.ini and fill in real values.
; db_config.ini is gitignored — NEVER commit credentials.
; Requires VPN access to reach the server.

[sqlserver]
server = YOUR_SERVER_NAME_OR_IP
database = YOUR_DATABASE_NAME
username = YOUR_USERNAME
password = YOUR_PASSWORD
; driver = ODBC Driver 17 for SQL Server
